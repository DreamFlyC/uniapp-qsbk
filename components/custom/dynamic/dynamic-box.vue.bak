<template>
	<view class="container b-b m-top-10">
		<view class="uni-flex-c">
			<!-- 用户头像 -->
			<view> <image class="topic-user-avatar" :src="item.userInfo.avatar" mode="widthFix" lazy-load="true"></image> </view>
			<!-- 用户信息 -->
			<view class="uni-flex-item">
				<view class="uni-flex-sbc">
					<!-- 用户昵称 -->
					<view>
						<!-- 上方名称 年龄信息 -->
						<view class="uni-flex">
							<view class="topic-user-name">{{item.userInfo.nickname}}</view> 
							<view class="user-sex-box"> 
								<view class="icon iconfont" :class="item.userInfo.sex == 1 ? 'icon-nan' : 'icon-nv'">{{item.userInfo.age}}</view> 
							</view>
						</view>
						<!-- 下方时间 -->	
						<view class="uni-flex">
							<view class="topic-user-desc">{{item.userInfo.time}}</view>
						</view>
					</view>
					<!-- 关注 -->
					<view class="uni-flex-c">
						<view class="topic-user-follow" v-if="!item.userInfo.follow" @click="onFollow()">关注</view>
						<view class="topic-user-del">×</view>
					</view>
				</view>
			</view>
		</view>
	</view>
</template>

<script>
export default {
	props: {
		item: Object
	},
	data() {
		return {
			itemData: this.item
		};
	}
};
</script>

<style lang="scss" scoped>
.topic-user-avatar {
	width: 70rpx;
	height: 70rpx;
	border-radius: 50%;
	margin-right: 20rpx;
}

.user-sex-box {
	background-color: #ec6f8b;
	border-radius: 20rpx;
	padding: 0rpx 5rpx;
	color: #ffffff;
	margin-left: 20rpx;
	.iconfont{
		font-size: 24rpx;
	}
}


.topic-user-follow {
			background-color: #f4f4f4;
			color: #000000;
			font-size: 24rpx;
			padding: 5rpx 30rpx;
			border-radius: 30rpx;
			margin-right: 30rpx;
		}
		.topic-user-del {
			color: #d7d7d7;
			font-size: 40rpx;
		}
		
		.topic-user-name {
			font-size: 30rpx;
			color: var(--bColor);
			font-weight: 500;
		}
		.topic-user-desc {
			font-size: 26rpx;
			color: #cccccc;
		}

</style>
